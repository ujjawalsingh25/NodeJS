
// npm i nodemon -D
// -D -->> It is for dev-dependencies that are for development environment it is not require for production
//          -->> While Production it will not install and reduce the application size


// Crypto Build in NodeJS package
    // -->> It provides a cryptographic functionality (like encryption, decryption)
    // -->> Used to hash passwords
    // **** When we add password with salt(secretKey type for all user differntly generated) then a map of Salt came 
    // and password will chnage of a String-Number combination which can be verify by using hash
    //  (password will be verified on hashmaped with User's salt.) 